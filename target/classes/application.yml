server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Aliissomeoneelse/Magazine-Market-Place.cloud-configuration.git
          username: Aliissomeeoneelse
          password: ${git.password}
        default-label: dev/Alisher