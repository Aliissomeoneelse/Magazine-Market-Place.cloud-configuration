server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Aliissomeoneelse/Magazine-Market-Place.properties.git
          username: SpaceXRM
          password: ${git.password}
        default-label: dev/Muhammadamin